---
description: 禁止覆盖用户已定稿的中文文案；改 CMS 只动用户点名的那一个字段
alwaysApply: true
---

# 汉格官网文案保护

前台文案以 CMS 已发布内容为准。用户在后台写过、改过、定过稿的中文（导语、卡片说明、正文、职位、页头古文）一律视为定稿。

## 禁止

- 不要用新写的句子替换 CMS / `site.ts` 里已有的中文导语、卡片说明、段落、职位描述。
- 不要整段覆盖 `contactPage`、`home`、`business`、`about`、`history` 等对象；只改用户点名的那一个字段。
- 不要在「改版面 / 改结构 / 改一个标题」时顺手重写周围文案。
- 不要把首页导语接到栏目页，或把栏目页古文接到首页。
- 不要用 ECS 脚本整文件回写 `draft.json` / `published.json`。必须改 JSON 时，只赋值被点名的键。

## 必须

- 用户说「改成 X」才把那一处改成 X；没给出新句子就不要动原文。
- 改标题（如「自荐」→「人才自荐」）只改 `applyTitle` 这一个键。
- 页头古文和首页导语是两套字段：`business.lead` 与 `home.fieldsLead`、`contactPage.lead` 与 `applyLead` 不要混用。
- 发现自己曾经覆盖过的定稿，先从 CMS / 对话记录找回原句再写回，找不到就停手等用户贴回，不要另写一句充数。
